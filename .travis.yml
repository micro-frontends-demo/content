language: node_js
node_js:
  - "8"

install: "echo 'This is static content - nothing to install!'"
script:
  - mkdir build
  - mv images build
  - mv style.css build

deploy:
  provider: s3
  bucket: content.demo.microfrontends.com
  local_dir: build
  skip_cleanup: true
